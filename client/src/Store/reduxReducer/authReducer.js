import { createSlice } from "@reduxjs/toolkit";

const authReducer = createSlice({
    name: ' auth',
    initialState: {

    },
    reducers: {

    },
    extraReducers: (builder) => {

    }

})
export const { /* export actions here */ } = authReducer.actions;
export default authReducer.reducer